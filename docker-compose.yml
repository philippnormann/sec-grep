services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "5002:5002"
    volumes:
      - sec-grep-data:/data
    restart: unless-stopped

volumes:
  sec-grep-data:
