services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "5002:5002"
    environment:
      - SEC_GREP_DB=/data/papers.db
    volumes:
      - sec-grep-data:/data
    restart: unless-stopped

volumes:
  sec-grep-data:
